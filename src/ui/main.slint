import { AboutSlint, VerticalBox } from "std-widgets.slint";

export component StoreWindow inherits Window 
{
    VerticalBox {
        Text 
        {
            text: "Hello World!";
        }
    }
}
