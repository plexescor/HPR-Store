export struct StoreItem_S {
    id: string,
    name: string,
    author: string,
    author-github: string,
    description: string,
    long-description: string,
    version: string,
    download-url: string,
    source-url: string,
    item-type: string,
    tags: [string],
    preview-images: [string],
    supported-hpr-versions: [string],
    download-count: int,
    star-count: int,
    last-updated: string,
}
